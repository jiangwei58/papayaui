# AI Agent 开发规则

## 语言规则

- 所有思考、计划、分析过程一律使用【中文】
- 所有最终回复、代码解释、注释一律使用【中文】
- 除非用户明确要求，否则不要使用英文
- 技术术语、命令、符号等保留英文原词，但解释必须是中文

## 编码规则

- 函数和 TypeScript 类型需要添加注释
- 不要删除已有注释，尤其是用户手写的行内注释；如果代码逻辑未改动，必须保留原注释
- 只有在对应代码被明确重写且原注释已经失效时，才允许同步调整注释内容，且应优先“更新”而不是“删除”
- 所有时间与日期处理统一使用 `dayjs`，不要新引入 `moment`、`date-fns` 等其他日期库，也不要在业务代码中直接用 `Date` 做复杂格式化和时区处理

## 前端开发约定

- 本仓库为 `papayaui` 组件库，前端默认使用 Vue 3 + TypeScript
- 编写 Vue 组件时优先使用 Composition API 与 `<script setup>` 写法
- 组件样式优先使用项目已有的 `scss` 方案（例如组件同名的 `.scss` 文件），无特殊需求不要额外引入新的全局样式体系
- 新增第三方 UI / 图标库前需要评估是否与现有组件库定位冲突

## 仓库目录说明（简称约定）

为了减少后文出现过多完整路径，这里先约定几个常用目录的简称：

- **组件库包（下文简称「组件库包」）**：`packages/papayaui/`
- **组件目录（下文简称「组件目录」）**：组件库包下的 `components/` 目录，即 `packages/papayaui/components/`
- **核心模块（下文简称「core 模块」）**：组件库包下的 `core/` 目录，即 `packages/papayaui/core/`
- **示例项目（下文简称「play 示例」）**：`play/`
- **文档站点（下文简称「docs 文档」）**：`docs/`

后续规则中，如无特别说明，均优先使用上述简称来指代对应目录，仅在需要时才写出完整路径。

## 命名规范

- 组件目录下：
  - 组件目录和 `.vue` 组件文件统一使用 kebab-case（如 `date-picker/`、`date-picker.vue`）
  - 组件相关的样式文件也使用 kebab-case，并与组件同名（如 `date-picker.scss`）
- 其他工具函数、hooks、store、配置等（不在组件目录下）统一使用 camelCase 命名文件与目录（如 `useDateRange.ts`、`dateUtils.ts`）
- 公共类型定义文件可根据所在目录跟随命名风格，但不应与已有规则冲突

## 依赖管理

- 仓库为 monorepo 结构，安装依赖时必须**进入对应子包目录或应用目录**再执行命令（例如进入组件库包、play 示例、docs 文档）
- 安装依赖统一使用 `pnpm add <package>` 命令，不要使用 npm 或 yarn 安装依赖
- 组件库相关依赖优先安装在组件库包下，play 示例与 docs 文档只安装自己运行所必需的依赖

## TypeScript 类型检查

- play 示例：在 `play` 目录下使用 `pnpm run type-check`
- 组件库包：在组件库包目录下使用 `pnpm run type-check`
- 其它包如果增加了类型检查脚本，应在对应目录下执行各自 `package.json` 中定义的 type-check 相关脚本，保持与现有脚本命名一致

## 单元测试

- 单元测试主要针对组件库包
- 如修改了 core 模块中的任意文件，**必须**在组件库包目录下执行 `pnpm test`
- 对组件库行为有影响的改动（即使不在 core 模块中）也应优先运行 `pnpm test`，必要时再补充运行 `pnpm run test-coverage`
